1. **Author the bootstrap catalog** — 6-8 doc-types, written as proper markdown templates with examples. Test each one in a real flow.
2. **Strip the runtime to the kernel** — remove the embedded code-pipeline opinions from the core. Make it a starter cell, not the product.
3. **Ship `flow new`** as the interactive builder. Linear chains only. Generate spike and step skills as output.
